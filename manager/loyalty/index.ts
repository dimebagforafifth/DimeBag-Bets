/**
 * Loyalty / progression config — a manager surface over the existing VIP program
 * (vip/ + app/vip-store). Reads + edits the live config through the program's own
 * guarded setters; adds no model and moves no money. Public surface.
 */

export { LoyaltyPage } from './ui/LoyaltyPage.js'
