A compact labelled readout box — a figure with an uppercase label. Pass `hot` to paint the value gold for a live multiplier or treasure figure.

```jsx
<Stat label="House edge" value="1.2%" />
<Stat label="Multiplier" value="14.7×" hot />
```

Values render in the rounded numeric face with `tabular-nums`. Group several in a flex/grid row.
