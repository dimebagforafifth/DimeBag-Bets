A small status pill. `gold` (outline) for Featured/tags, `solid` for emphasis like "Provably fair", `live` (green dot) for live status, `neutral` (grey) for quiet labels.

```jsx
<Badge variant="gold">Featured</Badge>
<Badge variant="live">Live</Badge>
<Badge variant="neutral">Originals</Badge>
```
